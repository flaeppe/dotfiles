# Auto generated by flake.nix devShell
.luarc.json

# The global ignore (~/.config/git/ignore) excludes .ctags.d/ to keep the
# generated copy in $HOME out of repos. Here it is source, and an untracked
# file is invisible to the flake -- which fails as a silently missing config
# rather than an error.
!nvim/.ctags.d/
!nvim/.ctags.d/*.ctags

# Same reasoning for the shared sqruff config: the global ignore excludes
# .sqruff so per-repo dialect overrides stay local, but here it is source.
!nvim/.sqruff

# The tool environment pyproject.toml describes. uv.lock is tracked; this is not.
.venv/

# AI
.claude/settings.local.json
result
