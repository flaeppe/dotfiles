---
description: Plan multi-step work with incremental decomposition
agent: plan
---
Load the `planning` skill before doing anything.

If a plan directory already exists for this project under `_private/.plan/`
or `~/anyfin/.plan/`, read the existing files (highest number first) to
understand current state before continuing.

$ARGUMENTS
